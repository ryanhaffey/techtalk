#Build nginx using docker-compose
docker-compose build

#run nginx image using docker-compose
#if prompted please allow docker to access your drive
docker-compose up

#browse to this page
http://localhost:5000/

#modify the index.html (located in your current directory)
#Find <h1>Welcome to Tech Talk - Docker!</h1> (located between the <body> </body> tags)
#Change the text between <h1> </h1> and save

#to see the changes refresh this page (if your changes are not displayed perform a hard refresh in your browser)
http://localhost:5000/ 

#Stop and remove the container and network
docker-compose down