#Build new nginx image
docker build -t nginx:techtalk . 

#run nginx image, map host port to 5000
docker run --name nginx_container -d -p 5000:80 nginx:techtalk

#browse to this page
http://localhost:5000/

#stop container 
docker stop nginx_container

#remove container
docker rm nginx_container