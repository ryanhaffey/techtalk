version: "3"
services:
    web:
      image: wordpress
      links:
        - mysql
      environment:
        - WORDPRESS_DB_PASSWORD=sample
      ports:
        - "8080:80"
    mysql:
      image: mysql:latest
      environment:
        - MYSQL_ROOT_PASSWORD=sample
        - MYSQL_DATABASE=wordpress